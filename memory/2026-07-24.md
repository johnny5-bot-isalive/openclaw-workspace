# 2026-07-24

## OpenClaw MiniMax retirement
- Jaret ended the MiniMax API subscription and asked to replace OpenClaw MiniMax usage with `openai/gpt-5.6-luna`.
- Updated active OpenClaw config so the default subagent model and Max use `openai/gpt-5.6-luna`.
- Removed active MiniMax provider/auth/plugin entries from `/home/jaret/.openclaw/openclaw.json` and per-agent model/auth JSON for Max, Grok, Sammy, and Hunter.
- Backups were written beside the edited files with suffix `.bak-20260724-183657-luna-replace`.

## OpenClaw GPT-5.6 Sol migration
- Jaret asked to add `gpt-5.6-sol` where OpenClaw was previously using `gpt-5.5`.
- Updated active OpenClaw config so the main default model, Sammy, Hunter, and their model overrides use `openai/gpt-5.6-sol`.
- Updated three disabled Idea Factory cron model overrides from `chatgpt-5.5` to `openai/gpt-5.6-sol`.
- Backups were written beside the edited files with suffix `.bak-20260724-190414-sol-replace`.
