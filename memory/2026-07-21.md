# 2026-07-21

## OpenClaw heartbeat failure investigation
- Heartbeat channel `agent:main:discord:channel:1493275133228351538` is configured to Main/Codex (`openai/gpt-5.5`); do not describe it as using Grok.
- The repeated failures around 14:32 and 15:03 PT came from separate session `agent:grok:main` using `xai/grok-4`.
- `agent:grok:main` was receiving `[OpenClaw heartbeat poll]` messages that referenced the heartbeat channel id, then failing before reply with xAI error `Could not decrypt the provided encrypted_content`, likely because its saved conversation state contains stale encrypted reasoning replay blobs.
- Likely fix path: reset/retire `agent:grok:main` or stop heartbeat polls from being delivered to that stale Grok lane; the Codex heartbeat lane itself looked healthy.

## Grok stale main-session cleanup
- Cleaned up stale Grok session key `agent:grok:main` after Jaret confirmed Grok should remain available for X/search use cases but unused poisoned state may be retired.
- Removed only `agent:grok:main` from `/home/jaret/.openclaw/agents/grok/sessions/sessions.json`; the Grok agent config remains intact (`xai/grok-4`, `x_search` allowed).
- Archived the retired transcript files under `/home/jaret/.openclaw/agents/grok/sessions/retired-20260721-1520-grok-main-heartbeat-poisoned/` and backed up the prior index as `/home/jaret/.openclaw/agents/grok/sessions/sessions.json.bak-20260721-1520-grok-main-heartbeat-poisoned`.
- Validation after cleanup: `openclaw sessions --agent grok --json --limit 20` returned zero sessions, and `openclaw config validate --json` returned valid with no warnings.
