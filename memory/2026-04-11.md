## 2026-04-11
- OpenClaw was updated successfully to version 2026.4.10, gateway came back cleanly, and `openclaw update status` reported up to date afterward.
- MiniMax M2.7 is now configured as the default subagent model for grunt work, while the main agent default remains `openai-codex/gpt-5.4`; the active Discord session needed to be refreshed to return to GPT after onboarding briefly made MiniMax the main default.
- The Obsidian Memory System reached a foundationally complete state: all top-level folders have `_index.md`, shared Nexus notes and Johnny-specific memory notes are in place, history conventions and the first daily log were created, and the vault is now in use/maintenance phase rather than architecture phase.
- A named MiniMax cleanup worker, Max, was used successfully for repeated Obsidian implementation passes; effective orchestration requires giving subagents explicit policy context, portability rules, current project state, and the expectation that Max should absorb grunt work by default when suitable.
- Graph strategy for the vault was refined after a failed first attempt: regular notes should prefer portable Markdown links, `_index.md` files are hidden structural folder contracts rather than graph hubs, and only a small number of explicitly named MOCs should act as visible graph hubs when justified.
- The assistant identified a memory-system weakness: durable context existed across workspace files and Obsidian, but execution commitments like QMD research and pending updates were not enforced strongly enough; follow-up work should add a small active-obligations layer and stronger heartbeat/task enforcement after the updater and QMD commitments.
- QMD research remains an explicit next commitment, targeting https://github.com/tobi/qmd for implementation planning.
- Memory-system hardening should avoid over-customization and bloat: prefer native Active Memory for retrieval, keep local workspace memory lightweight and execution-focused, use Obsidian for durable collaborative/project memory, and avoid duplication across both layers unless the benefit is clear.
- Obsidian vault path must be remembered exactly: Windows `G:\My Drive\Obsidian Vaults\Second Brain`, WSL `/mnt/g/My Drive/Obsidian Vaults/Second Brain`.
- The canonical persistent place for local environment-specific paths like the Obsidian vault is `TOOLS.md`; the Second Brain path was added there so it is not buried only in daily memory notes.
- A lightweight software-team style execution system was added to the Obsidian vault, then refactored the same day from project-local boards into a global system to avoid cross-project fragmentation. The canonical execution surfaces are now `40 Agent Nexus/Backlog.md` and `40 Agent Nexus/Kanban.md`.
- Heartbeat behavior was completed and normalized: `HEARTBEAT.md` now directs heartbeat to work active Johnny-owned Kanban items first and, if none exist, pull the next eligible backlog item into the Kanban and start it immediately.
- After reviewing the local QMD planning notes, the global backlog was expanded with the remaining QMD implementation items needed for full functionality: corpus definition, install/init, collection/context setup, indexing/embedding, retrieval evaluation, operational decision, and downstream workflow integration.
- Backlog ownership was then refined so Max is the default owner for QMD grunt-work tasks like install/init, collection setup, indexing/embedding, and retrieval evaluation, while Johnny retains architectural and policy decisions.
