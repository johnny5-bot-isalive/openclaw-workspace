- Installed .NET SDK 8.0.420 locally under `/home/jaret/.dotnet` because root apt installation was unavailable from this session; updated `~/.profile` and `~/.bashrc` to export `DOTNET_ROOT` and prepend `~/.dotnet` to `PATH` for future shells.
- Scaffolded `/home/jaret/repos/aspnet-react-starter` as a ready-to-run full-stack starter: ASP.NET Core 8 Web API backend, React 19 + Vite + TypeScript frontend, fixed local dev ports (`5181` API, `5173` frontend), Vite proxying for `/api`, root `Makefile`, root `package.json` with `npm run dev`, xUnit API smoke tests, and GitHub Actions CI setup.
- Validated the starter end to end with `make setup`, `make ci`, `npm run dev`, `curl http://localhost:5181/api/status`, and a frontend dev-server fetch path through Vite.
- Jaret's intended workflow for the first real app is roadmap -> collaborative PRD -> product backlog, then standardized process refinements learned during delivery. Expected delegation pattern: use focused subagents for lower-level execution-heavy work like unit tests and CI/CD pipeline wiring while Johnny stays on higher-level product and integration work.
- Jaret wants workflow and memory to stay markdown-first with Obsidian as the durable source of truth. For the planned web app, prefer a thin action/launcher surface over duplicating inbox, backlog, status, or memory data into a second system that would require ongoing sync.
- Scaffolded a new Obsidian project for `Mission Control` under `40 Agent Nexus/Projects/Mission Control/`, including `Project Brief.md`, `Product Requirements Document.md`, `Operating Notes.md`, `Project Backlog.md`, and `Project Kanban.md`. The project is now associated with Discord channel `#mission-control-project` (`agent:main:discord:channel:1494479860540903434`) in the registry, while cadence remains manual.
- Studied `garrytan/gbrain` in depth and wrote an inbox memo at `/mnt/c/Users/Jaret/Obsidian/The Nexus/00 Inbox/GBrain study report 2026-04-16.md`. Main conclusion: borrow ideas, not a wholesale replacement. Strongest candidates are explicit brain-vs-memory-vs-session rules, compiled-truth-plus-timeline note structure, and a possible future Postgres/pgvector retrieval layer as a secondary index while keeping Obsidian Markdown-first.
- Turned Jaret's inbox note `Passive income generation.md` into a new Obsidian project, `Idea Factory`, under `40 Agent Nexus/Projects/Idea Factory/`, with a cleaned brainstorming note, local backlog, local Kanban, registry entry, scoring rubric, research funnel, and daily ranked-ideas template. The current active task is running the first scored market scan.
