- Heartbeat board access to the Obsidian Second Brain vault recovered after earlier ENODEV mount failures; Kanban and Backlog were readable again later in the day.
- OpenCode Setup work entered the global board on 2026-04-14. G-028 was completed: OpenCode ACP is reachable from the WSL-hosted OpenClaw gateway with no immediate auth or adapter blocker.
- Max pulled G-029 into Doing: verify GitHub tooling and repo-home readiness for OpenCode work. Current blocker is that `gh auth` was not logged in. Next step is `gh auth login`, then verify `gh auth status`, confirm `/home/jaret/repos/` readiness, and prepare a clean local repo path.
- For the current GitHub setup flow, the recommended path is HTTPS rather than SSH, and when `gh auth login` asks "Authenticate Git with your GitHub credentials?" the recommended answer is yes.
- After G-029, the next backlog items are G-030 (repo baseline for OpenCode), G-031 (enable Diffs plugin for review-oriented editing), and G-032 (later evaluate optional OpenCode hardening after at least one successful repo workflow).
- Heartbeat board access to the Obsidian Second Brain vault recovered after earlier ENODEV mount failures; Kanban and Backlog were readable again later in the day.
- OpenCode Setup work entered the global board on 2026-04-14. G-028 was completed: OpenCode ACP is reachable from the WSL-hosted OpenClaw gateway with no immediate auth or adapter blocker.
- Max pulled G-029 into Doing: verify GitHub tooling and repo-home readiness for OpenCode work. Current blocker is that `gh auth` was not logged in. Next step is `gh auth login`, then verify `gh auth status`, confirm `/home/jaret/repos/` readiness, and prepare a clean local repo path.
- For the current GitHub setup flow, the recommended path is HTTPS rather than SSH, and when `gh auth login` asks "Authenticate Git with your GitHub credentials?" the recommended answer is yes.
- After G-029, the next backlog items are G-030 (repo baseline for OpenCode), G-031 (enable Diffs plugin for review-oriented editing), and G-032 (later evaluate optional OpenCode hardening after at least one successful repo workflow).
- Later on 2026-04-14, G-029 was completed and the earlier `gh auth` blocker was cleared. Durable state: `gh auth` is active on github.com as `johnny5-bot-isalive` with scopes `gist`, `read:org`, `repo`, and `workflow`.
- The dedicated local repo home for OpenCode-managed work is now `/home/jaret/repos/opencode-repos`.
- G-030 became the active OpenCode item. A baseline repo exists at `/home/jaret/repos/opencode-repos/opencode-test-repo`, and it has already been published to `https://github.com/johnny5-bot-isalive/opencode-test-repo`.
- During G-030, the baseline repo was improved to be a reusable starter: `scripts/check-env.sh` was fixed so an empty `.env.example` does not fail under `set -euo pipefail`, a `Makefile` was added with reproducible `env-check`, `lint`, `test`, and `ci` targets, the `README.md` was expanded with baseline usage, and CI/test-glob behavior was tightened. `make env-check` and `make ci` now run successfully locally.
- The Diffs plugin is now explicitly enabled in OpenClaw config. As a result, G-031 was tightened from "enable Diffs" to "verify an end-to-end diff-review flow now that Diffs is enabled."
- Current OpenCode execution state at flush time: G-030 is still in Doing, the most recent local repo refinements are not yet pushed, and the recommended next move is to keep `opencode-test-repo` as the reusable baseline/template first, then create the first real app repo from that baseline.
- Jaret's durable preference is that repos should live directly under `/home/jaret/repos` regardless of IDE or harness. The earlier `/home/jaret/repos/opencode-repos` subfolder was rejected as unnecessary. The baseline repo was moved to `/home/jaret/repos/opencode-test-repo`, and tooling defaults should target the shared repos root going forward.
- G-030 was completed using `opencode-test-repo` as the reusable starter template. Durable baseline facts: the repo now includes formatter and lint config (`.prettierrc.json`, `.prettierignore`, `.markdownlint.json`), a safer env-check script, a template smoke test (`scripts/test-template.sh`), a broader Makefile command set (`setup`, `env-check`, `format`, `format-check`, `lint`, `test`, `typecheck`, `build`, `dev`, `ci`), and a harness script that defaults to `/home/jaret/repos` while removing any copied local `.env`. Local commit: `2f2566e` (`Finish reusable baseline template for future repos`).
- G-031 was initially blocked because the `diffs` plugin was enabled globally but the `diffs` tool was not exposed inside Max subagent sessions. That blocker was removed without changing tool policy: the validated fallback review path for this setup is `git diff --stat`, `git diff --unified`, plus ordinary file reads from the main session when a worker lacks the `diffs` tool. This fallback was documented in `opencode-test-repo` README and committed locally as `01c9c3a` (`Document fallback diff-review path`), and G-031 was marked done.
- Jaret approved the future workflow direction of one control-plane heartbeat plus project-scoped sessions and local boards. The durable implementation plan now lives in `40 Agent Nexus/Projects/Project-Scoped Workflow/`, and OpenCode Setup is designated as the first pilot once the registry and template layer are created.
- The project-scoped workflow plan was then split into concrete backlog items G-034 through G-040, covering registry creation, template/ownership rules, dispatch contract, OpenCode pilot bootstrap, card migration, heartbeat dispatch changes, and pilot validation.
