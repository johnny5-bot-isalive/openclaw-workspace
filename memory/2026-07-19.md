# 2026-07-19

- Weekly `[CRON-MEMORY-HYGIENE]` reviewed recent July daily memory and promoted only durable operational items into `MEMORY.md`: Tavily as configured web search provider, per-agent `models.json`/MiniMax `apiKey: "***"` handling, and the Codex cron rule to avoid restrictive `payload.toolsAllow` and untested per-job model overrides when filesystem/shell access is needed.
