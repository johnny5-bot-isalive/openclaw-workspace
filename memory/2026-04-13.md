- Heartbeat checks on 2026-04-13 showed the gateway service still running OpenClaw 2026.4.10 while update status indicated 2026.4.12 available.
- `openclaw status` showed heartbeat still enabled for `main` on a 30 minute cadence with last status `ok-token`; the issue Jaret noticed is more likely visible-delivery/config drift than a stopped heartbeat.
- Jaret upgraded to a higher ChatGPT subscription tier and then asked whether that caused heartbeat to stop; current evidence suggests the subscription change was not the cause.
- OpenClaw was updated successfully to 2026.4.12. Official release notes reviewed afterward.
- Most relevant 2026.4.12 changes for this setup: bundled Codex provider/app-server harness support for `codex/gpt-*`; Active Memory and QMD recall reliability improvements (including better default QMD search behavior and cleaner default memory/QMD handling); cron/heartbeat reliability fixes (isolated cron config persistence, transcript-path fixes, stale Discord heartbeat timer cleanup); and a CLI update fix for stale post-update runtime imports.
- MEMORY.md was cleaned up to reflect the real cron schedule: daily update check at 9:00 AM PT, daily retrospective at 12:10 AM PT, and workspace sync at 3:00 AM PT, with the old one-time 2026.4.9 hold note effectively retired.
- Heartbeat checks on 2026-04-13 showed the gateway service still running OpenClaw 2026.4.10 while update status indicated 2026.4.12 available.
- `openclaw status` showed heartbeat still enabled for `main` on a 30 minute cadence with last status `ok-token`; the issue Jaret noticed is more likely visible-delivery/config drift than a stopped heartbeat.
- Jaret upgraded to a higher ChatGPT subscription tier and then asked whether that caused heartbeat to stop; current evidence suggests the subscription change was not the cause.
- OpenClaw was updated successfully to 2026.4.12. Official release notes reviewed afterward.
- Most relevant 2026.4.12 changes for this setup: bundled Codex provider/app-server harness support for `codex/gpt-*`; Active Memory and QMD recall reliability improvements (including better default QMD search behavior and cleaner default memory/QMD handling); cron/heartbeat reliability fixes (isolated cron config persistence, transcript-path fixes, stale Discord heartbeat timer cleanup); and a CLI update fix for stale post-update runtime imports.
- MEMORY.md was cleaned up to reflect the real cron schedule: daily update check at 9:00 AM PT, daily retrospective at 12:10 AM PT, and workspace sync at 3:00 AM PT, with the old one-time 2026.4.9 hold note effectively retired.
- Dream mode is enabled in `~/.openclaw/openclaw.json` under `plugins.entries.memory-core.config.dreaming.enabled = true`.
- The managed dream job is present and enabled in `~/.openclaw/cron/jobs.json` as `Memory Dreaming Promotion`, scheduled for `0 3 * * *`, which currently resolves to 3:00 AM America/Los_Angeles on this host.
- Jaret wants the daily retrospective to stay narrowly scoped as a curated human-facing closeout that does not duplicate the internal `memory/YYYY-MM-DD.md` notes.
- New standing workflow preference: whenever Johnny is waiting on Jaret for approval, action, or a decision, the request should be surfaced in `00 Inbox` so it is visible in the human action queue.
- Jaret approved the weekly `MEMORY.md` hygiene cron. It was implemented as the OpenClaw cron job `Weekly MEMORY hygiene review`, scheduled for Sundays at 09:30 America/Los_Angeles, with main-session system event `[CRON-MEMORY-HYGIENE]` and conservative promotion/pruning rules for `MEMORY.md`.
- Heartbeat checks on 2026-04-13 showed the gateway service still running OpenClaw 2026.4.10 while update status indicated 2026.4.12 available.
- `openclaw status` showed heartbeat still enabled for `main` on a 30 minute cadence with last status `ok-token`; the issue Jaret noticed is more likely visible-delivery/config drift than a stopped heartbeat.
- Jaret upgraded to a higher ChatGPT subscription tier and then asked whether that caused heartbeat to stop; current evidence suggests the subscription change was not the cause.
- OpenClaw was updated successfully to 2026.4.12. Official release notes reviewed afterward.
- Most relevant 2026.4.12 changes for this setup: bundled Codex provider/app-server harness support for `codex/gpt-*`; Active Memory and QMD recall reliability improvements (including better default QMD search behavior and cleaner default memory/QMD handling); cron/heartbeat reliability fixes (isolated cron config persistence, transcript-path fixes, stale Discord heartbeat timer cleanup); and a CLI update fix for stale post-update runtime imports.
- MEMORY.md was cleaned up to reflect the real cron schedule: daily update check at 9:00 AM PT, daily retrospective at 12:10 AM PT, and workspace sync at 3:00 AM PT, with the old one-time 2026.4.9 hold note effectively retired.
- Heartbeat checks on 2026-04-13 showed the gateway service still running OpenClaw 2026.4.10 while update status indicated 2026.4.12 available.
- `openclaw status` showed heartbeat still enabled for `main` on a 30 minute cadence with last status `ok-token`; the issue Jaret noticed is more likely visible-delivery/config drift than a stopped heartbeat.
- Jaret upgraded to a higher ChatGPT subscription tier and then asked whether that caused heartbeat to stop; current evidence suggests the subscription change was not the cause.
- OpenClaw was updated successfully to 2026.4.12. Official release notes reviewed afterward.
- Most relevant 2026.4.12 changes for this setup: bundled Codex provider/app-server harness support for `codex/gpt-*`; Active Memory and QMD recall reliability improvements (including better default QMD search behavior and cleaner default memory/QMD handling); cron/heartbeat reliability fixes (isolated cron config persistence, transcript-path fixes, stale Discord heartbeat timer cleanup); and a CLI update fix for stale post-update runtime imports.
- MEMORY.md was cleaned up to reflect the real cron schedule: daily update check at 9:00 AM PT, daily retrospective at 12:10 AM PT, and workspace sync at 3:00 AM PT, with the old one-time 2026.4.9 hold note effectively retired.
- Dream mode is enabled in `~/.openclaw/openclaw.json` under `plugins.entries.memory-core.config.dreaming.enabled = true`.
- The managed dream job is present and enabled in `~/.openclaw/cron/jobs.json` as `Memory Dreaming Promotion`, scheduled for `0 3 * * *`, which currently resolves to 3:00 AM America/Los_Angeles on this host.
- Jaret wants the daily retrospective to stay narrowly scoped as a curated human-facing closeout that does not duplicate the internal `memory/YYYY-MM-DD.md` notes.
- New standing workflow preference: whenever Johnny is waiting on Jaret for approval, action, or a decision, the request should be surfaced in `00 Inbox` so it is visible in the human action queue.
- Jaret approved the weekly `MEMORY.md` hygiene cron. It was implemented as the OpenClaw cron job `Weekly MEMORY hygiene review`, scheduled for Sundays at 09:30 America/Los_Angeles, with main-session system event `[CRON-MEMORY-HYGIENE]` and conservative promotion/pruning rules for `MEMORY.md`.
- For the remaining Discord slash-command audit warning, the practical minimal fix is to set `channels.discord.dm.allowFrom` to include Jaret's Discord user id; restart the gateway after saving, then rerun the audit.
- The session-list growth is mainly stale `agent:main:subagent:*` heartbeat-spawned runs in main's session store, not many persistent Max sessions; Max's own store currently only showed one session, `agent:max:main`.
- Likely cause of session buildup: heartbeat instructions spawn a subagent every run, while `sessions_spawn` defaults to `cleanup: "keep"` and best-effort auto-archive timers can be lost across restarts.
- Preferred prevention plan for heartbeat-spawned Max work: use `agentId: "max"`, `mode: "run"`, and `cleanup: "delete"`, with optional shorter `archiveAfterMinutes` and a `runTimeoutSeconds` safeguard.
- Preferred cleanup plan for existing stale subagent sessions is backup-first pruning of stale `agent:main:subagent:*` entries from main's session store plus matching transcript archival/rename, only after prevention changes are in place.
- `HEARTBEAT.md` was updated so Max is spawned as a disposable one-shot worker via `sessions_spawn` with `runtime: "subagent"`, `agentId: "max"`, `mode: "run"`, and `cleanup: "delete"` to reduce stale heartbeat-spawned session buildup.
- Cleaned the main session store after backup by removing stale heartbeat/delegation `agent:main:subagent:*` records and the old Telegram direct session, archiving their transcripts to `*.deleted.<timestamp>`; the main session store was reduced to only the live Discord/main entries.
- The first `HEARTBEAT.md` change to spawn Max with `agentId: "max"` exposed that `sessions_spawn` only allows targeting the requester agent by default; the fix is to add `agents.defaults.subagents.allowAgents = ["max"]` in `~/.openclaw/openclaw.json` so main may explicitly spawn Max.
- Jaret decided to use OpenCode rather than Codex for local coding harness work. A concrete setup plan for a local OpenCode plus GitHub development environment was written to Obsidian Inbox at `00 Inbox/OpenCode Local Coding Environment Plan.md`, recommending ACP runtime with explicit `acp` config, WSL-local repo checkouts, GitHub CLI workflow, and enabling the Diffs plugin once the core loop works.
- An implementation checklist was then created in Obsidian Inbox at `00 Inbox/OpenCode Setup Implementation Checklist.md`. The first live setup step was completed by adding an explicit `acp` block to `~/.openclaw/openclaw.json` with `enabled: true`, `backend: "acpx"`, `defaultAgent: "opencode"`, and `allowedAgents: ["opencode"]`; the next required step is a gateway restart before running an OpenCode ACP smoke test.
- Jaret later moved the OpenCode planning notes into `40 Agent Nexus/Projects/OpenCode Setup/`. After the gateway restart, the remaining OpenCode implementation work was converted into heartbeat-ready backlog items G-028 through G-032, with grunt-work phases assigned to Max and the initial OpenCode ACP bring-up/hardening decisions assigned to Johnny 5.
- G-028 was then executed immediately: a minimal ACP smoke test against `agentId: "opencode"` succeeded, confirming the OpenCode harness is reachable from the WSL-hosted gateway with no immediate auth or adapter blocker. The moved OpenCode checklist was updated to mark the smoke test and minimal auth/runtime path as verified.
