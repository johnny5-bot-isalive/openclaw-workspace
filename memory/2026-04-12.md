# 2026-04-12

- QMD was adopted as a local retrieval layer for workspace and Obsidian notes on this machine.
- Default QMD usage here should be CPU-only: `QMD_LLAMA_GPU=off`.
- Preferred retrieval split is now: QMD for fuzzy semantic cross-note search, `memory_search` for OpenClaw runtime recall, and grep/read for exact lookup.
- MCP exposure for QMD is deferred for now; CLI-first usage is the current operating model.
- `openclaw doctor --non-interactive` reported a gateway service config mismatch: the service entrypoint still points at `dist/entry.js` while the current install expects `dist/index.js`.
- Doctor also reported 2 orphan transcript files in `~/.openclaw/agents/main/sessions` that can be safely archived, plus startup optimization suggestions (`NODE_COMPILE_CACHE`, `OPENCLAW_NO_RESPAWN=1`).
- No channel security warnings were reported by doctor.


# 2026-04-12 (continued)

- QMD was adopted as a local retrieval layer for workspace and Obsidian notes on this machine.
- Default QMD usage here should be CPU-only: `QMD_LLAMA_GPU=off`.
- Preferred retrieval split is now: QMD for fuzzy semantic cross-note search, `memory_search` for OpenClaw runtime recall, and grep/read for exact lookup.
- MCP exposure for QMD is deferred for now; CLI-first usage is the current operating model.
- `openclaw doctor --non-interactive` reported a gateway service config mismatch: the service entrypoint still points at `dist/entry.js` while the current install expects `dist/index.js`.
- Doctor also reported 2 orphan transcript files in `~/.openclaw/agents/main/sessions` that can be safely archived, plus startup optimization suggestions (`NODE_COMPILE_CACHE`, `OPENCLAW_NO_RESPAWN=1`).
- No channel security warnings were reported by doctor.

## Heartbeat investigation (2026-04-12)

- Ran `openclaw system heartbeat last` to diagnose heartbeat execution.
- First heartbeat test run: Max (subagent) successfully executed on G-004 alignment work.
- Second heartbeat run: Did real work (Max spawned, G-012 completed) but returned `status: "skipped", reason: "target-none"`.
- **Root cause of "target-none"**: heartbeat ran the `HEARTBEAT.md` instructions and produced a result, but had no valid delivery target for that result. The work happened; the delivery did not.
- **Fix direction**: configure a default target for heartbeat results. The standing orders docs mention "deliver results to the provided target or configured default" — so a `defaultTarget` or equivalent config setting for heartbeat is the likely solution.
- `openclaw system heartbeat last` is the correct diagnostic command; `openclaw status` does not show heartbeat timing.
- Still investigating: what config controls heartbeat result delivery target.
- QMD pilot completed successfully on the memory-project corpus. QMD is now adopted as a local retrieval layer, CLI-first, with CPU-only usage (`QMD_LLAMA_GPU=off`) as the default on this machine. MCP integration is explicitly deferred.
- A real OpenClaw agent `max` was created with identity name Max, emoji 🧰, and model `minimax/MiniMax-M2.7`. CLI execution of the `max` agent works, but `sessions_spawn` in this runtime rejected explicit `agentId`, so heartbeat-targeted Max delegation remains constrained by tool/runtime policy.
- The global backlog/Kanban process drifted out of sync multiple times. A durable process fix was adopted: any board-state change must reconcile Backlog and Kanban in the same turn, re-read both before reporting, and treat Max as an audit/helper rather than the primary correctness mechanism.
- Jaret wants note-search behavior to reliably prefer QMD first for fuzzy semantic retrieval across workspace memory and the Obsidian vault, while `memory_search` remains for native runtime recall and `grep`/`read` remain for exact lookup.
- Jaret requested two new Max-owned backlog tasks: (1) apply 2 to 3 broad folder-based domain tags to Obsidian notes, excluding `00 Inbox`, `01 System`, and `99 Archive`; (2) remove YAML properties from ordinary Obsidian notes, preserving them only in `_index` files and MOCs.
# 2026-04-12

- QMD was adopted as a local retrieval layer for workspace and Obsidian notes on this machine.
- Default QMD usage here should be CPU-only: `QMD_LLAMA_GPU=off`.
- Preferred retrieval split is now: QMD for fuzzy semantic cross-note search, `memory_search` for OpenClaw runtime recall, and grep/read for exact lookup.
- MCP exposure for QMD is deferred for now; CLI-first usage is the current operating model.
- `openclaw doctor --non-interactive` reported a gateway service config mismatch: the service entrypoint still points at `dist/entry.js` while the current install expects `dist/index.js`.
- Doctor also reported 2 orphan transcript files in `~/.openclaw/agents/main/sessions` that can be safely archived, plus startup optimization suggestions (`NODE_COMPILE_CACHE`, `OPENCLAW_NO_RESPAWN=1`).
- No channel security warnings were reported by doctor.


# 2026-04-12 (continued)

- QMD was adopted as a local retrieval layer for workspace and Obsidian notes on this machine.
- Default QMD usage here should be CPU-only: `QMD_LLAMA_GPU=off`.
- Preferred retrieval split is now: QMD for fuzzy semantic cross-note search, `memory_search` for OpenClaw runtime recall, and grep/read for exact lookup.
- MCP exposure for QMD is deferred for now; CLI-first usage is the current operating model.
- `openclaw doctor --non-interactive` reported a gateway service config mismatch: the service entrypoint still points at `dist/entry.js` while the current install expects `dist/index.js`.
- Doctor also reported 2 orphan transcript files in `~/.openclaw/agents/main/sessions` that can be safely archived, plus startup optimization suggestions (`NODE_COMPILE_CACHE`, `OPENCLAW_NO_RESPAWN=1`).
- No channel security warnings were reported by doctor.

## Heartbeat investigation (2026-04-12)

- Ran `openclaw system heartbeat last` to diagnose heartbeat execution.
- First heartbeat test run: Max (subagent) successfully executed on G-004 alignment work.
- Second heartbeat run: Did real work (Max spawned, G-012 completed) but returned `status: "skipped", reason: "target-none"`.
- **Root cause of "target-none"**: heartbeat ran the `HEARTBEAT.md` instructions and produced a result, but had no valid delivery target for that result. The work happened; the delivery did not.
- **Fix direction**: configure a default target for heartbeat results. The standing orders docs mention "deliver results to the provided target or configured default" — so a `defaultTarget` or equivalent config setting for heartbeat is the likely solution.
- `openclaw system heartbeat last` is the correct diagnostic command; `openclaw status` does not show heartbeat timing.
- Still investigating: what config controls heartbeat result delivery target.
- QMD pilot completed successfully on the memory-project corpus. QMD is now adopted as a local retrieval layer, CLI-first, with CPU-only usage (`QMD_LLAMA_GPU=off`) as the default on this machine. MCP integration is explicitly deferred.
- A real OpenClaw agent `max` was created with identity name Max, emoji 🧰, and model `minimax/MiniMax-M2.7`. CLI execution of the `max` agent works, but `sessions_spawn` in this runtime rejected explicit `agentId`, so heartbeat-targeted Max delegation remains constrained by tool/runtime policy.
- The global backlog/Kanban process drifted out of sync multiple times. A durable process fix was adopted: any board-state change must reconcile Backlog and Kanban in the same turn, re-read both before reporting, and treat Max as an audit/helper rather than the primary correctness mechanism.
- Jaret wants note-search behavior to reliably prefer QMD first for fuzzy semantic retrieval across workspace memory and the Obsidian vault, while `memory_search` remains for native runtime recall and `grep`/`read` remain for exact lookup.
- Jaret requested two new Max-owned backlog tasks: (1) apply 2 to 3 broad folder-based domain tags to Obsidian notes, excluding `00 Inbox`, `01 System`, and `99 Archive`; (2) remove YAML properties from ordinary Obsidian notes, preserving them only in `_index` files and MOCs.
- The saved QMD CLI reference note is at `/mnt/g/My Drive/Obsidian Vaults/Second Brain/20 Library/QMD CLI Commands.md`; future QMD test planning should read that note instead of guessing the path.
- A heartbeat-reported QMD audit failure was traced to first-run `qmd query` behavior: it triggered a reranker model download and then failed before returning results, but the underlying QMD setup was not broken.
- The reranker model completed download successfully afterward, and `QMD_LLAMA_GPU=off qmd query "active obligations layer" --json -n 5` then completed with exit code 0 and returned usable ranked results.
- Jaret also independently confirmed normal QMD CLI behavior by running `qmd search "Kanban"`, which returned the correct Obsidian vault results.
- Durable takeaway: on this machine, prefer `QMD_LLAMA_GPU=off qmd search ...` for quick checks and use `QMD_LLAMA_GPU=off qmd query ...` when better ranking is worth the extra runtime now that the reranker is cached.


# 2026-04-12 (late session)

## Daily retrospective cron job (approved by Jaret)
- Jaret approved the daily retrospective cron job design in concept
- Cron should run at 00:10 America/Los_Angeles daily
- Inputs: Kanban (primary), daily log for closing day, relevant working notes, all Discord channel sessions from last 24 hours (not just current channel)
- Outputs: close/update daily log, create backlog items only when durable follow-up is warranted, archive completed Kanban items to `99 Archive/Kanban Archive/YYYY-MM.md`
- Drop "backlog changes" as an input; treat backlog as output target only
- Silent by default unless failure, major blocker, or significant new follow-up created
- Next step: implement the cron job and standing order

## Backlog cleanup directive
- Jaret noted tasks not being removed from Backlog once copied to Kanban
- Max completed a board reconciliation pass removing done items from Backlog
- G-014 and G-016 were the stale Backlog items already marked done but not removed — now confirmed cleaned

## Max G-018 progress (this session)
- Created `10 Spaces/Personal/Context Prompts/_index.md` from template
- Flagged 3 folders still needing indexes: `01 System/Templates`, `01 System/MOCs`, `40 Agent Nexus/Agents/Johnny 5`
- Flagged `40 Agent Nexus/Decisions` as empty with unclear purpose, recommend deletion
- Card updated to doing in Kanban and in-progress in Backlog

## Current board state at close
- Johnny Doing: G-019 (QMD CLI validation)
- Max Doing: G-018 (folder index audit)
- Backlog: G-020 (Johnny, ready), G-021 (Johnny, ready), G-003 (Johnny, backlog), G-005 (Johnny, backlog)
