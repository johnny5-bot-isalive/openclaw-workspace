# TOOLS.md - Local Notes

Skills define _how_ tools work. This file is for Hunter-specific local notes.

## Browser

- OpenClaw browser is available to Hunter in this environment.
- Preferred local browser wrapper: `/home/jaret/.local/bin/openclaw-chrome-local`
- Current validated capabilities inherited from Sammy's setup: browser start, direct tab open, Playwright-backed `navigate`, `snapshot` with `format=aria`, and `snapshot` with `format=ai`.
- Use browser only as a bounded exception lane after lighter research surfaces fail materially.
